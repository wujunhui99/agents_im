# Copy to: secret/k8s_access
# Keep this as a local note only. Do not commit real host/user/credential values.
ACCESS_METHOD=ssh-to-server-then-kubectl
NAMESPACE=agents-im
SAFE_CHECKS="kubectl -n agents-im get pods; kubectl -n agents-im rollout status deploy/web"
