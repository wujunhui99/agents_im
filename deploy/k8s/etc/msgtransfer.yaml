Name: msgtransfer
WorkerID: ${HOSTNAME}
DryRun: false
StorageDriver: postgres
DataSource: ${DATABASE_URL}

Dispatcher:
  Driver: gateway
  GatewayEndpoint: http://127.0.0.1:8084

Worker:
  PollIntervalMillis: 100
  RetryBackoffMillis: 1000
  MaxAttempts: 5

Observability:
  Enabled: true
  Host: 0.0.0.0
  Port: ${MESSAGE_TRANSFER_OBSERVABILITY_PORT}

# Kafka 链路是唯一消费路径（03 §9 B3b：legacy outbox consumer 已退役）；
# Redis 经 REDIS_ADDR/REDIS_PASSWORD env 解析。
Kafka:
  Enabled: true
  Brokers: redpanda.agents-im.svc.cluster.local:9092
