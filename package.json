{
  "scripts": {
    "frontend:dev": "npm --prefix web run dev",
    "frontend:build": "npm --prefix web run build",
    "frontend:test": "npm --prefix web run test:run",
    "frontend:lint": "npm --prefix web run lint"
  },
  "devDependencies": {}
}
